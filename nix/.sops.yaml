# Your key:  age-keygen -o ~/.config/sops/age/keys.txt
# Host key:  ssh-keyscan homelab | ssh-to-age
keys:
  - &admin age1replace-me-admin
  - &homelab age1replace-me-homelab
creation_rules:
  - path_regex: secrets/[^/]+\.yaml$
    key_groups:
      - age:
          - *admin
          - *homelab
