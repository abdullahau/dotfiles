- defaults:
    link:
      relink: true

- clean: ["~", "~/.config"]

- create:
   - ~/Developer
   - ~/.config

- link:
    # Shell
    ~/.zshrc: zsh/zshrc
    ~/.zshenv: zsh/zshenv
    ~/.p10k.zsh: zsh/p10k.zsh
    ~/.inputrc: zsh/inputrc
    ~/.zfunc: zsh/zfunc
    # Git
    ~/.gitconfig: git/gitconfig
    ~/.gitignore_global: git/gitignore_global
    # Terminal Packages & App Configs
    ~/.config/micro: micro
    ~/.config/yazi: yazi
    ~/.config/bat: bat
    ~/.config/btop: btop
    ~/.config/zellij: zellij
    ~/.config/atuin: atuin
    ~/.claude/CLAUDE.md: claude/CLAUDE.md
    # SSH
    ~/.ssh/config: ssh/config
    # Linters
    ~/.config/codebook: codebook
    ~/.config/ruff: ruff

- shell:
  - command: sudo apt-get update && sudo apt-get install -y zsh
    stdin: true
    stdout: true
    stderr: true
  - command: [chsh -s $(which zsh), Making zsh the default shell]
    stdin: true
    stdout: true
    stderr: true
  - command: ./setup_ubuntu.zsh
    stdin: true
    stdout: true
    stderr: true
  - command: ./setup_homebrew.zsh
    stdin: true
    stdout: true
    stderr: true
  - command: ./setup_docker.zsh
    stdin: true
    stdout: true
    stderr: true
  # No args here on purpose: setup_vps_relay.sh takes none — it sources .env
  # itself and reads TARGET_IP/RELAY_PORTS from there.
  - command: ./setup_vps_relay.sh
    stdin: true
    stdout: true
    stderr: true
  - command: ./setup_vps_local_ports.sh
    stdin: true
    stdout: true
    stderr: true
  - command: ./setup_bbr.sh
    stdin: true
    stdout: true
    stderr: true
